{
 "batch": "aln-epicurves.tr",
 "lang": "tr",
 "kind": "align",
 "results": [
  {
   "id": "aln-epicurves.tr#1",
   "verdict": "fixed",
   "reason": "Inserted English chunk 46 (the ggplot(central_data) example using scales::label_date_short()) verbatim after the existing Turkish paragraph that introduces label_date_short() and before the 'Ikinci bir secenek' paragraph; the Turkish file lacked it while the introducing prose was already present, so no new prose was added.",
   "file": "chapters/epicurves.tr.qmd",
   "old": "etiketlerin kendiliğinden ayarlanmasıdır.\n\nİkinci bir seçenek de yüzey oluşturmayı kullanmaktır.",
   "new": "etiketlerin kendiliğinden ayarlanmasıdır.\n\n```{r, warning=F}\nggplot(central_data) +\n\n  # histogram\n  geom_histogram(\n    mapping = aes(x = date_onset),\n    breaks = weekly_breaks_central,   # pre-defined date vector - see top of ggplot section\n    closed = \"left\",                  # count cases from start of breakpoint\n    color = \"darkblue\",\n    fill = \"lightblue\") +\n\n  # y-axis scale as before\n  scale_y_continuous(expand = c(0,0))+\n\n  # x-axis scale sets efficient date labels\n  scale_x_date(\n    expand = c(0,0),                      # remove excess x-axis space below and after case bars\n    labels = scales::label_date_short())+ # auto efficient date labels\n\n  # labels and theme\n  labs(\n    title = \"Using label_date_short()\\nTo make automatic and efficient date labels\",\n    x = \"Week of symptom onset\",\n    y = \"Weekly case indicence\")+\n  theme_minimal()\n```\n\n\nİkinci bir seçenek de yüzey oluşturmayı kullanmaktır."
  }
 ]
}
