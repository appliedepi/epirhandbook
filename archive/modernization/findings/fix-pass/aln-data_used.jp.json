{
 "batch": "aln-data_used.jp",
 "lang": "jp",
 "kind": "align",
 "results": [
  {
   "id": "aln-data_used.jp#1",
   "verdict": "fixed",
   "reason": "In the case linelist section the Japanese had three chunks (raw_linelist, linelist, linelist_excel) where English has one, so I deleted the linelist and linelist_excel chunks with the lead-in sentences that only introduced them, and also the lead-in that only introduced raw_linelist, because the English carries all three loads as comments inside the single surviving chunk.",
   "file": "chapters/data_used.jp.qmd",
   "old": "。「加工前の」症例ラインリストは、雑多なデータを含む Excel のスプレッドシートです。[データクリーニングと主要関数](cleaning.jp.qmd) の章を学ぶ際にご利用ください。\n\n```{r, eval=F}\nraw_linelist <- appliedepidata::get_data(name = \"case_linelists_linelist_raw\")\n```\n\n。このファイルは、このハンドブック内でラインリストを使用するすべての章で使用します。.rds ファイルは、列のデータ型を保持する R 特有のファイル形式です。これにより、R にデータをインポートした後、最小限のクリーニングを行うだけでよくなります。\n\n```{r, eval=F}\nlinelist <- appliedepidata::get_data(name = \"linelist_cleaned_rds\")\n```\n\n<u>その他の関連ファイル</u>\n\n* Excel ファイル形式の「前処理された」ラインリスト。\n\n```{r, eval=F}\nlinelist_excel <- appliedepidata::get_data(name = \"linelist_cleaned_excel\")\n```\n",
   "new": "```{r, eval=F}\nraw_linelist <- appliedepidata::get_data(name = \"case_linelists_linelist_raw\")\n```\n"
  },
  {
   "id": "aln-data_used.jp#2",
   "verdict": "fixed",
   "reason": "In the phylogenetic trees section the Japanese had three chunks (tree, sample_data, subtree) where English has one, so I deleted the sample_data and subtree chunks and the three one-line Japanese lead-ins that only introduced them, keeping the tree chunk at the English chunk's position.",
   "file": "chapters/data_used.jp.qmd",
   "old": "系統樹（**ape** パッケージの `phylo` オブジェクト）：\n\n```{r, eval=F}\ntree <- appliedepidata::get_data(name = \"Shigella_tree\")\n```\n\n各サンプルの追加情報：\n\n```{r, eval=F}\nsample_data <- appliedepidata::get_data(name = \"sample_data_Shigella_tree\")\n```\n\nこの章の後半で作成するサブセット・ツリー：\n\n```{r, eval=F}\nsubtree <- appliedepidata::get_data(name = \"Shigella_subtree_2\")\n```\n",
   "new": "```{r, eval=F}\ntree <- appliedepidata::get_data(name = \"Shigella_tree\")\n```\n"
  },
  {
   "id": "aln-data_used.jp#3",
   "verdict": "fixed",
   "reason": "In the survey analysis section the Japanese had three chunks (survey_data, survey_dict, population) where English has one, so I deleted the survey_dict and population chunks, which had no introducing prose of their own.",
   "file": "chapters/data_used.jp.qmd",
   "old": "```{r, eval=F}\nsurvey_data <- appliedepidata::get_data(name = \"survey_data\")\n```\n\n```{r, eval=F}\nsurvey_dict <- appliedepidata::get_data(name = \"survey_dict\")\n```\n\n```{r, eval=F}\npopulation <- appliedepidata::get_data(name = \"population\")\n```\n",
   "new": "```{r, eval=F}\nsurvey_data <- appliedepidata::get_data(name = \"survey_data\")\n```\n"
  }
 ]
}