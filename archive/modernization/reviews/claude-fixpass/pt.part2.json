{
  "lang": "pt",
  "part": 2,
  "hunks_total": 78,
  "hunks_read": 78,
  "items": [
    {
      "file": "chapters/iteration.pt.qmd",
      "severity": "broken-markup",
      "new_text": "`for (item em sequência) {fazer operações usando item}`",
      "problem": "The hunk rewrote the second half of this line (colchetes -> chaves) so that it tells the reader to look at the parentheses and curly brackets of this syntax line, but the syntax line itself has the R keyword `in` translated to `em`, so it is not valid R. The English is `for (item in sequence) {do operations using item}`. Pre-existing, not introduced by the hunk, but it sits in the hunk's own NEW text and the edited clause now points straight at it.",
      "proposed_fix": "`for (item in sequência) {fazer operações usando item}`"
    },
    {
      "file": "chapters/tables_presentation.pt.qmd",
      "severity": "broken-markup",
      "new_text": "utilizando a função `align()` de **flextable****.",
      "problem": "The rewritten sentence ends with four asterisks. Pandoc closes the bold at `**flextable**` and then renders the remaining `**` literally, so the page prints 'flextable**.'. This is the only `****` in the file, and the fix agent rewrote this exact line without repairing it.",
      "proposed_fix": "utilizando a função `align()` de **flextable**."
    },
    {
      "file": "chapters/iteration.pt.qmd",
      "severity": "broken-markup",
      "new_text": "(`média no grupo f` e `média no grupo m`)",
      "problem": "These are the literal names R prints in the `estimate` element of a `t.test()` result, so they must stay `mean in group f` and `mean in group m`; the code chunk below produces those English labels and the prose names labels that never appear. Pre-existing on the line the hunk rewrote (lista de classes -> classe lista); the same translated pair also sits at line 729.",
      "proposed_fix": "(`mean in group f` e `mean in group m`)"
    },
    {
      "file": "chapters/reportfactory.pt.qmd",
      "severity": "broken-markup",
      "new_text": "* Dentro de \"saídas\", subpastas foram criadas para cada relatório Rmd",
      "problem": "This is the literal folder name \"outputs\" translated, which is exactly the defect r_projects.pt#1 fixed in this same batch. It is two lines above the changed line inside the hunk's own context, and the same file already writes the folder as \"outputs\" at line 51. English: 'Within \"outputs\", sub-folders have been created for each Rmd report'.",
      "proposed_fix": "* Dentro de \"outputs\", subpastas foram criadas para cada relatório Rmd"
    },
    {
      "file": "chapters/standardization.pt.qmd",
      "severity": "wrong-meaning",
      "new_text": "# importe dados demográficos do país A diretamente do Github",
      "problem": "standardization.pt#2 removed the false 'do nosso repositório Github' claim from the prose, but the five chunk comments directly below still say 'diretamente do Github' while the calls are `appliedepidata::get_data()`, which reads from the installed package. The English comments carry no GitHub claim ('# demographics for country A'). The fix agent was right not to edit inside the chunk, so this needs an owner decision.",
      "proposed_fix": "Drop 'diretamente do Github' from the five comments at lines 59, 62, 65, 68 and 71, or 'revert' the prose fix is not an option: the prose is now correct and only the comments are stale."
    }
  ]
}
