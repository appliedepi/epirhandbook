{"batch":"aln-data_used.pt","lang":"pt","kind":"align","results":[{"id":"aln-data_used.pt#1","verdict":"fixed","reason":"Merged the three separate case-linelist chunks (raw_linelist, linelist, linelist_excel) into the single chunk the English has, and deleted the Portuguese prose that only introduced the two extra chunks.","file":"chapters/data_used.pt.qmd","old":"A linelist do caso \"bruta\" é uma planilha do Excel com dados confusos. Use-o para acompanhar a página [Limpeza de dados e funções principais](cleaning.pt.qmd).\n\n```{r, eval=F}\nraw_linelist <- appliedepidata::get_data(name = \"case_linelists_linelist_raw\")\n```\n\nSe você quiser acompanhar, carregue os dados em R:\n\nUse este arquivo para todas as outras páginas deste livro que usam a lista de linha. Um arquivo .rds é um tipo de arquivo específico de R que preserva classes de coluna.\n\n```{r, eval=F}\nlinelist <- appliedepidata::get_data(name = \"linelist_cleaned_rds\")\n```\n\n*Outros arquivos relacionados:*  \n\n* A linelist \"limpa\", como arquivo Excel.\n\n```{r, eval=F}\nlinelist_excel <- appliedepidata::get_data(name = \"linelist_cleaned_excel\")\n```\n\n* Parte da página de limpeza usa um \"dicionário de limpeza\" (arquivo .csv). Você pode carregá-lo diretamente no R executando os seguintes comandos:   ","new":"```{r, eval=F}\nraw_linelist <- appliedepidata::get_data(name = \"case_linelists_linelist_raw\")\nlinelist <- appliedepidata::get_data(name = \"linelist_cleaned_rds\")\nlinelist_excel <- appliedepidata::get_data(name = \"linelist_cleaned_excel\")\n```\n\nParte da página de limpeza usa um \"dicionário de limpeza\" (arquivo .csv). Você pode carregá-lo diretamente no R executando os seguintes comandos:"},{"id":"aln-data_used.pt#2","verdict":"fixed","reason":"Merged the three separate phylogenetic-tree chunks (tree, sample_data, subtree) into the single chunk the English has, turning each deleted introductory sentence into the code comment the English carries.","file":"chapters/data_used.pt.qmd","old":"A árvore filogenética, como objeto `phylo` (pacote **ape**):\n\n```{r, eval=F}\ntree <- appliedepidata::get_data(name = \"Shigella_tree\")\n```\n\nInformações adicionais sobre cada amostra:\n\n```{r, eval=F}\nsample_data <- appliedepidata::get_data(name = \"sample_data_Shigella_tree\")\n```\n\nA sub-árvore criada mais adiante na página:\n\n```{r, eval=F}\nsubtree <- appliedepidata::get_data(name = \"Shigella_subtree_2\")\n```","new":"```{r, eval=F}\n# a árvore filogenética, como objeto \"phylo\" (pacote ape)\ntree <- appliedepidata::get_data(name = \"Shigella_tree\")\n\n# informações adicionais sobre cada amostra\nsample_data <- appliedepidata::get_data(name = \"sample_data_Shigella_tree\")\n\n# a sub-árvore criada mais adiante na página\nsubtree <- appliedepidata::get_data(name = \"Shigella_subtree_2\")\n```"},{"id":"aln-data_used.pt#3","verdict":"fixed","reason":"Merged the three separate survey-analysis chunks (survey_data, survey_dict, population) into the single chunk the English has, adding the English code comments in Portuguese.","file":"chapters/data_used.pt.qmd","old":"```{r, eval=F}\nsurvey_data <- appliedepidata::get_data(name = \"survey_data\")\n```\n\n```{r, eval=F}\nsurvey_dict <- appliedepidata::get_data(name = \"survey_dict\")\n```\n\n```{r, eval=F}\npopulation <- appliedepidata::get_data(name = \"population\")\n```","new":"```{r, eval=F}\n# dados fictícios da pesquisa\nsurvey_data <- appliedepidata::get_data(name = \"survey_data\")\n\n# dicionário de dados fictício da pesquisa\nsurvey_dict <- appliedepidata::get_data(name = \"survey_dict\")\n\n# dados populacionais fictícios da pesquisa\npopulation <- appliedepidata::get_data(name = \"population\")\n```"}]}
