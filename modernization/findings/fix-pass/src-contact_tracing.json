{"batch":"src-contact_tracing","lang":"en","kind":"source","results":[{"id":"src-contact_tracing#1","verdict":"fixed","reason":"Three sentences wrote the row count as inline code with a leading space instead of an inline R expression, so the page printed the literal call text; changed the leading space to r in each, and cases, contacts and followups are all defined in the chunk immediately above their sentence.","file":"chapters/contact_tracing.qmd","old":"Here are the ` nrow(cases)` cases:  \nHere are the ` nrow(contacts)` rows of the `contacts` dataset:  \nHere are the first 50 rows of the ` nrow(followups)`-row `followups` dataset (each row is a follow-up interaction, with outcome status in the `followup_status` column):  ","new":"Here are the `r nrow(cases)` cases:  \nHere are the `r nrow(contacts)` rows of the `contacts` dataset:  \nHere are the first 50 rows of the `r nrow(followups)`-row `followups` dataset (each row is a follow-up interaction, with outcome status in the `followup_status` column):  "}]}
