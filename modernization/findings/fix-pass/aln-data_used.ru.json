{
 "batch": "aln-data_used.ru",
 "lang": "ru",
 "kind": "align",
 "results": [
  {
   "id": "aln-data_used.ru#1",
   "verdict": "fixed",
   "reason": "Merged the three separate case-linelist chunks (raw_linelist, linelist, linelist_excel) into the single English chunk and deleted the three Russian sentences and the bullet header that only introduced the deleted chunks.",
   "file": "chapters/data_used.ru.qmd",
   "old": "\"Сырой\" построчный список случаев - таблица Excel с хаотичными данными. Используйте ее, чтобы выполнять шаги на странице [Вычистка данных и ключевые функции](cleaning.ru.qmd).\n\n```{r, eval=F}\nraw_linelist <- appliedepidata::get_data(name = \"case_linelists_linelist_raw\")\n```\n\nИспользуйте этот файл для всех других страниц данного руководства, где используется построчный список. Файл .rds - это конкретный тип файла для R, который сохраняет классы столбцов.\n\n```{r, eval=F}\nlinelist <- appliedepidata::get_data(name = \"linelist_cleaned_rds\")\n```\n\n*Другие связанные файлы:*  \n\n* «Чистый» построчный список в виде файла Excel.\n\n```{r, eval=F}\nlinelist_excel <- appliedepidata::get_data(name = \"linelist_cleaned_excel\")\n```\n\n* Часть страницы вычистки использует",
   "new": "```{r, eval=F}\n# the \"raw\" linelist - an Excel spreadsheet with messy data\n# use this to follow along with the Cleaning data and core functions page\nraw_linelist <- appliedepidata::get_data(name = \"case_linelists_linelist_raw\")\n\n# the \"clean\" linelist - an R-specific .rds file that preserves column classes\n# use this for all other pages of this handbook that use the linelist\nlinelist <- appliedepidata::get_data(name = \"linelist_cleaned_rds\")\n\n# the \"clean\" linelist, as an Excel file instead\nlinelist_excel <- appliedepidata::get_data(name = \"linelist_cleaned_excel\")\n```\n\nЧасть страницы вычистки использует"
  },
  {
   "id": "aln-data_used.ru#2",
   "verdict": "fixed",
   "reason": "Merged the three separate phylogenetic-tree chunks (tree, sample_data, subtree) into the single English chunk and deleted the three Russian lead-in sentences that only introduced them.",
   "file": "chapters/data_used.ru.qmd",
   "old": "Филогенетическое дерево, объект `phylo` (пакет **ape**):\n\n```{r, eval=F}\ntree <- appliedepidata::get_data(name = \"Shigella_tree\")\n```\n\nДополнительная информация по каждому образцу:\n\n```{r, eval=F}\nsample_data <- appliedepidata::get_data(name = \"sample_data_Shigella_tree\")\n```\n\nПоддерево, создаваемое далее на этой странице:\n\n```{r, eval=F}\nsubtree <- appliedepidata::get_data(name = \"Shigella_subtree_2\")\n```",
   "new": "```{r, eval=F}\n# the phylogenetic tree, as a \"phylo\" object (ape package)\ntree <- appliedepidata::get_data(name = \"Shigella_tree\")\n\n# additional information on each sample\nsample_data <- appliedepidata::get_data(name = \"sample_data_Shigella_tree\")\n\n# the subset-tree created later in the page\nsubtree <- appliedepidata::get_data(name = \"Shigella_subtree_2\")\n```"
  },
  {
   "id": "aln-data_used.ru#3",
   "verdict": "fixed",
   "reason": "Merged the three separate survey chunks (survey_data, survey_dict, population) into the single English chunk; they had no introducing prose to delete.",
   "file": "chapters/data_used.ru.qmd",
   "old": "```{r, eval=F}\nsurvey_data <- appliedepidata::get_data(name = \"survey_data\")\n```\n\n```{r, eval=F}\nsurvey_dict <- appliedepidata::get_data(name = \"survey_dict\")\n```\n\n```{r, eval=F}\npopulation <- appliedepidata::get_data(name = \"population\")\n```",
   "new": "```{r, eval=F}\n# fictional survey data\nsurvey_data <- appliedepidata::get_data(name = \"survey_data\")\n\n# fictional survey data dictionary\nsurvey_dict <- appliedepidata::get_data(name = \"survey_dict\")\n\n# fictional survey population data\npopulation <- appliedepidata::get_data(name = \"population\")\n```"
  }
 ]
}
