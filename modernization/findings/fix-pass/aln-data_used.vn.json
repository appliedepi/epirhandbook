{"batch":"aln-data_used.vn","lang":"vn","kind":"align","results":[{"id":"aln-data_used.vn#1","verdict":"fixed","reason":"Merged the three separate linelist chunks (raw_linelist, linelist, linelist_excel) into the one chunk English chunk 5 has, deleting the two Vietnamese sentences and the bullet list that only introduced the second and third of them.","file":"chapters/data_used.vn.qmd","old":"Bộ dữ liệu \"thô\" là một trang tính Excel với dữ liệu lộn xộn. Sử dụng số liệu này trong chương [Làm sạch số liệu và các hàm quan trọng](cleaning.vn.qmd).\n\n```{r, eval=F}\nraw_linelist <- appliedepidata::get_data(name = \"case_linelists_linelist_raw\")\n```\n\nSử dụng tệp này cho tất cả các chương khác trong sổ tay có sử dụng bộ dữ liệu linelist. Tệp mở rộng .rds là một kiểu file của R có khả năng lưu trữ các thông tin cột.\n\n```{r, eval=F}\nlinelist <- appliedepidata::get_data(name = \"linelist_cleaned_rds\")\n```\n\n*Các tệp liên quan khác:*\n\n* Dữ liệu linelist \"đã làm sạch\" dưới dạng tệp Excel.\n\n```{r, eval=F}\nlinelist_excel <- appliedepidata::get_data(name = \"linelist_cleaned_excel\")\n```\n","new":"```{r, eval=F}\nraw_linelist <- appliedepidata::get_data(name = \"case_linelists_linelist_raw\")\n\nlinelist <- appliedepidata::get_data(name = \"linelist_cleaned_rds\")\n\nlinelist_excel <- appliedepidata::get_data(name = \"linelist_cleaned_excel\")\n```\n"},{"id":"aln-data_used.vn#2","verdict":"fixed","reason":"Merged the three separate phylogenetic-tree chunks (tree, sample_data, subtree) into the one chunk English chunk 10 has, deleting the three Vietnamese label sentences that only introduced them.","file":"chapters/data_used.vn.qmd","old":"Cây phả hệ, dưới dạng đối tượng `phylo` (package **ape**):\n\n```{r, eval=F}\ntree <- appliedepidata::get_data(name = \"Shigella_tree\")\n```\n\nThông tin bổ sung cho từng mẫu:\n\n```{r, eval=F}\nsample_data <- appliedepidata::get_data(name = \"sample_data_Shigella_tree\")\n```\n\nCây con được tạo ở phần sau của chương:\n\n```{r, eval=F}\nsubtree <- appliedepidata::get_data(name = \"Shigella_subtree_2\")\n```\n","new":"```{r, eval=F}\ntree <- appliedepidata::get_data(name = \"Shigella_tree\")\n\nsample_data <- appliedepidata::get_data(name = \"sample_data_Shigella_tree\")\n\nsubtree <- appliedepidata::get_data(name = \"Shigella_subtree_2\")\n```\n"},{"id":"aln-data_used.vn#3","verdict":"fixed","reason":"Merged the three separate survey chunks (survey_data, survey_dict, population) into the one chunk English chunk 14 has; no prose separated them.","file":"chapters/data_used.vn.qmd","old":"```{r, eval=F}\nsurvey_data <- appliedepidata::get_data(name = \"survey_data\")\n```\n\n```{r, eval=F}\nsurvey_dict <- appliedepidata::get_data(name = \"survey_dict\")\n```\n\n```{r, eval=F}\npopulation <- appliedepidata::get_data(name = \"population\")\n```\n","new":"```{r, eval=F}\nsurvey_data <- appliedepidata::get_data(name = \"survey_data\")\n\nsurvey_dict <- appliedepidata::get_data(name = \"survey_dict\")\n\npopulation <- appliedepidata::get_data(name = \"population\")\n```\n"},{"id":"aln-data_used.vn#4","verdict":"fixed","reason":"Un-bulleted the cleaning-dictionary sentence, which was left as an orphaned list item after the bullet list introducing it was deleted in #1; English carries it as a plain paragraph.","file":"chapters/data_used.vn.qmd","old":"-   Một phần của chương làm sạch sử dụng \"từ điển làm sạch\" (tệp .csv). Bạn có thể tải nó trực tiếp vào R bằng cách chạy các lệnh sau:","new":"Một phần của chương làm sạch sử dụng \"từ điển làm sạch\" (tệp .csv). Bạn có thể tải nó trực tiếp vào R bằng cách chạy các lệnh sau:"}]}
